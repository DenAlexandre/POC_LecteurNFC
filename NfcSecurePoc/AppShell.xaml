<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:NfcSecurePoc.Views"
       x:Class="NfcSecurePoc.AppShell"
       Title="NFC Secure POC">

    <TabBar>
        <ShellContent Title="HCE" ContentTemplate="{DataTemplate views:HcePage}" Icon="{x:Null}" />
        <ShellContent Title="Reader" ContentTemplate="{DataTemplate views:ReaderPage}" Icon="{x:Null}" />
    </TabBar>

</Shell>
